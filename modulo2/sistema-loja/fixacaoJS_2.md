```javascript
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if(quantidade < 12){
    let total = quantidade * 1.30
    return totalDaCompra
  } else {
    let totalComDuzia = quantidade * 1.00
    return totalComDuzia
  }
}```